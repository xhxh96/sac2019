import { ImageStyle, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imageContainer: {
    borderRadius: 12,
    height: 45,
    width: 45
  } as ImageStyle
});

export default styles;
